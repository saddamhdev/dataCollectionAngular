import { FilterGlobalPipe } from './filter-global.pipe';

describe('FilterGlobalPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterGlobalPipe();
    expect(pipe).toBeTruthy();
  });
});
