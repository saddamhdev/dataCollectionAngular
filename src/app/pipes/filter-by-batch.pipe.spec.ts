import { FilterByBatchPipe } from './filter-by-batch.pipe';

describe('FilterByBatchPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterByBatchPipe();
    expect(pipe).toBeTruthy();
  });
});
